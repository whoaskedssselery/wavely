## Скачать

**Windows** — `wavely Setup *.exe` (обычный инсталлятор) или `wavely *.exe` без "Setup" в имени (portable, без установки).

**Linux** — `wavely-*.AppImage`. Сделать исполняемым и запустить:

```
chmod +x wavely-*.AppImage
./wavely-*.AppImage
```

---
